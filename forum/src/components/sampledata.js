export const markdownSample = `
$$\\begin{equation}
l = \\int_{\\alpha}^{\\beta} \\sqrt{ (x'(t))^2 + (y'(t))^2}dt
\\end{equation}
$$

$$
\\begin{equation}
\\text{Cov}(X, Y) = \\text{E}((X - \\text{E}X)(Y-\\text{E}Y))
\\end{equation}
$$

$$ \\text{Correlation coef}= \\frac{Cov(X,Y)}{\\sigma_X\\cdot\\sigma_Y}$$

1. Teraz obliczmy $\\int_0^1 x^{100}dx$
2. Potem coś tam
\t\`\`\`javascript
\tconsole.log("hello there");
\tfunction integral(f, a, b) {
\t\tconst n = 1000;
\t\tconst h = (b-a) / n; 
\t}
\t\`\`\`
`;
